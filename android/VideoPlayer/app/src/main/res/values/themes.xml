<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VideoPlayer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="NextButton" parent="ExoStyledControls.Button.Center.Next">
        <item name="android:src">@drawable/next_drawable</item>
    </style>

    <style name="PlayPauseButton" parent="ExoStyledControls.Button.Center.PlayPause">
        <item name="android:src">@drawable/play_drawable</item>
    </style>

    <style name="PreviousButton" parent="ExoStyledControls.Button.Center.Previous">
        <item name="android:src">@drawable/previous_drawable</item>
    </style>
</resources>